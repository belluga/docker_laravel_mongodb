# Well-Known Contract Examples

These files are reference contracts only.

- They describe the payload shape expected for `/.well-known/assetlinks.json`
  and `/.well-known/apple-app-site-association`.
- They are not the runtime source of truth for a downstream project.
- Keep project-specific identifiers, fingerprints, and public path inventories
  in downstream-owned config or backend logic.
